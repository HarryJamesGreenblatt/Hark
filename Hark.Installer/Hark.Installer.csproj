<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net9.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <!-- Custom entry point (Program.Main) so the elevated cert-only relaunch is handled before any
         window shows; no App.xaml, so this stays the single Main. -->
    <StartupObject>Hark.Installer.Program</StartupObject>
    <!-- Per-monitor v2 DPI awareness so the WPF dialog scales correctly on high-DPI laptops. -->
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <AssemblyName>Hark-Setup</AssemblyName>
    <Product>HARK Setup</Product>
    <AssemblyTitle>HARK Setup</AssemblyTitle>
    <Description>Installer for HARK — live captions overlay</Description>
    <Authors>Harry Greenblatt</Authors>
    <ApplicationIcon>..\Hark.App\Assets\Icon.ico</ApplicationIcon>
  </PropertyGroup>

  <ItemGroup>
    <!-- Public signing cert (committed) — trusted during install so the signed MSIX validates. -->
    <EmbeddedResource Include="Hark.cer">
      <LogicalName>Hark.cer</LogicalName>
    </EmbeddedResource>
    <!-- HAL-eye logo for the installer chrome. -->
    <EmbeddedResource Include="..\Hark.App\Assets\Icon.png">
      <LogicalName>Icon.png</LogicalName>
    </EmbeddedResource>
    <!-- The signed package is copied in and embedded by CI just before this exe is published;
         absent during a normal dev build (the installer then reports it can't find the package). -->
    <EmbeddedResource Include="Hark.msix" Condition="Exists('Hark.msix')">
      <LogicalName>Hark.msix</LogicalName>
    </EmbeddedResource>
    <!-- Infra Bicep (single source of truth) embedded so the installer can run the same subscription
         deployment the provisioning pipeline does. Extracted to a temp folder at provision time. -->
    <EmbeddedResource Include="..\infra\main.bicep">
      <LogicalName>infra.main.bicep</LogicalName>
    </EmbeddedResource>
    <EmbeddedResource Include="..\infra\modules\speech.bicep">
      <LogicalName>infra.modules.speech.bicep</LogicalName>
    </EmbeddedResource>
    <EmbeddedResource Include="..\infra\modules\openai.bicep">
      <LogicalName>infra.modules.openai.bicep</LogicalName>
    </EmbeddedResource>
  </ItemGroup>

</Project>
